using UnityEngine;

public class SendMessage : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //LserialHandler.Write("M"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
